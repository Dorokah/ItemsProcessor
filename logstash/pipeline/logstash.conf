input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    data_stream => false
    index => "%{project}-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
